---
phase: 04-首页标签交互筛选
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - docs/.vitepress/theme/components/HomeTagCloud.vue
  - docs/.vitepress/theme/components/FilteredDocList.vue
  - docs/.vitepress/theme/index.ts
  - docs/index.md
  - docs/.vitepress/theme/custom.css
autonomous: true
requirements:
  - HOME-04
  - HOME-05
  - HOME-06
must_haves:
  truths:
    - User clicks a tag chip on the homepage -> document list filters to show only documents tagged with that tag
    - User clicks additional tags -> documents narrow via AND logic (doc must have ALL selected tags)
    - Selected tag chips display box-shadow ring + brightness active state (D-06), distinct from unselected chips
    - User clicks an already-selected tag -> deselects it and the document list expands
    - "清除" button appears when tags are selected; clicking it deselects all tags at once
    - When no tags are selected, the full document list is displayed
    - When no documents match all selected tags, "没有匹配标签的文档" empty state message is shown (D-04)
    - Filtered list items animate with fade+slide transitions (D-05)
    - Tag cloud stats text updates to "选中 Z 个标签：显示 N 篇文档" when tags selected (D-08), restores to "共 X 个标签，覆盖 Y 篇文档" when no tags selected
  artifacts:
    - docs/.vitepress/theme/components/FilteredDocList.vue
  key_links:
    - HomeTagCloud click events call toggleTag from shared composable -> state changes propagate to FilteredDocList
    - FilteredDocList renders reactively from composable's filteredDocs computed
    - index.md replaces static markdown list with FilteredDocList component tag
---

<objective>
在 HomeTagCloud 组件中实现标签点击交互，创建 FilteredDocList 组件替代静态 Markdown 文档列表，完成多标签 AND 筛选、选中高亮、清除按钮、动态统计文字和过渡动画。

Purpose: 实现所有用户面向的交互功能——让首页标签变得可点击，文档列表根据标签选择动态过滤，提供视觉反馈和交互动效。

Output:
- 更新后的 HomeTagCloud.vue（可点击标签、动态统计、清除按钮）
- 新建 FilteredDocList.vue（过滤后的文档列表组件）
- 更新后的 theme/index.ts（注册 FilteredDocList）
- 更新后的 index.md（FilteredDocList 替代 Markdown 列表）
- 更新后的 custom.css（TransitionGroup 动画关键帧）
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-首页标签交互筛选/04-CONTEXT.md
@docs/.vitepress/theme/util/tagChip.ts
@docs/.vitepress/theme/util/useHomeTagFilter.ts
@docs/.vitepress/theme/components/HomeTagCloud.vue
@docs/.vitepress/theme/components/VPLocalSearchBox.vue
@docs/.vitepress/theme/index.ts
@docs/index.md
@docs/.vitepress/theme/custom.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 HomeTagCloud 组件添加标签点击交互和动态 UI</name>
  <files>docs/.vitepress/theme/components/HomeTagCloud.vue</files>
  <read_first>
    @docs/.vitepress/theme/components/HomeTagCloud.vue
    @docs/.vitepress/theme/util/useHomeTagFilter.ts
    @docs/.vitepress/theme/components/VPLocalSearchBox.vue
  </read_first>
  <action>
    Modify HomeTagCloud.vue to integrate the composable and add click interaction per D-01, D-06, D-07, D-08.

    Script changes:
    - Import `useHomeTagFilter` from `../util/useHomeTagFilter`
    - Destructure `{ selectedTags, toggleTag, clear, isEmpty, filteredCount, selectedCount, allTags }` from `useHomeTagFilter()` — note: `allTags` is not returned by the composable currently (it is the component's own computation). Keep the existing `allTags` computed from `allTagsFromIndex`. The composable provides `selectedTags`, `toggleTag`, `clear`, `filteredDocs`, `selectedCount`, `filteredCount`, `isEmpty`. Add `totalDocs` as a separate computed: `const totalDocs = computed(() => Object.keys(tagIndex as Record<string, { tags: string[], title: string }>).length)` — keep the existing one from Task 1 in Plan 01.
    - Import `ref` and `computed` from `vue` (already imported)
    - Optionally import `filteredCount` or compute locally - check what the composable returns

    Template changes:
    - Add `@click="toggleTag(tag.name)"` to the button element
    - Add `:class="{ active: selectedTags.has(tag.name) }"` to the button
    - Add `:aria-pressed="selectedTags.has(tag.name) ? 'true' : 'false'"` accessibility attribute
    - Replace the static stats line:
      `<p class="hc-stats">共 {{ allTags.length }} 个标签，覆盖 {{ totalDocs }} 篇文档</p>`
      with a reactive version that shows:
      - No tags selected: keep original text
      - Tags selected: `"选中 Z 个标签：显示 N 篇文档"` (D-08 logic: when selectedTags.size > 0, show `选中 ${selectedTags.size} 个标签：显示 ${filteredCount} 篇文档`)
    - Add a "清除" button (D-07) that appears when `selectedTags.size > 0`, styled similarly to VPLocalSearchBox's `.tag-chip.tag-clear` pattern, positioned after the tag chips in the .hc-tag-list. Use `@click="clear"`. Style it as a small transparent button with secondary text color.

    Scoped CSS additions:
    - Add `.tag-chip.active` rule matching D-06: `box-shadow: 0 0 0 2px currentColor; filter: brightness(1.1);` — this must exactly match VPLocalSearchBox's `.tag-chip.active` selector (lines 982-985 in VPLocalSearchBox.vue)
    - Change the existing `.tag-chip` cursor from `default` to `pointer` since tags are now clickable
    - Add `.tag-chip.tag-clear` style matching VPLocalSearchBox's clear button pattern (lines 987-993): transparent background, secondary text color, small font size, hover effect

    Import additions needed:
    - `useHomeTagFilter` import
  </action>
  <acceptance_criteria>
    - Tag buttons respond to click events via composable toggleTag
    - Clicked tag shows `.active` class with box-shadow ring + brightness(1.1) (D-06)
    - aria-pressed attribute reflects selected state
    - Stats text shows "选中 Z 个标签：显示 N 篇文档" when tags selected (D-08)
    - Stats text restores to original when no tags selected
    - "清除" button appears only when tags selected (D-07)
    - "清除" button clears all selected tags on click
    - Tag chip cursor shows pointer instead of default
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    HomeTagCloud tags are clickable with visual active state, dynamic stats text, and a clear-all button. Build passes without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 FilteredDocList 组件</name>
  <files>docs/.vitepress/theme/components/FilteredDocList.vue</files>
  <read_first>
    @docs/index.md
    @docs/.vitepress/theme/util/useHomeTagFilter.ts
  </read_first>
  <action>
    Create a new file `docs/.vitepress/theme/components/FilteredDocList.vue` per D-02, D-04, D-05.

    Script section:
    - Import `useHomeTagFilter` from `../util/useHomeTagFilter`
    - Import `tagIndex` from `virtual:tag-index` for initial doc source
    - Destructure `{ filteredDocs, isEmpty }` from `useHomeTagFilter()`
    - Optionally import `useData` from `vitepress` and `computed`/`ref` from `vue` as needed

    Template:
    - Render a `<div class="FilteredDocList vp-doc">` wrapper to inherit VitePress doc styling
    - Inside, use `<TransitionGroup name="list" tag="ul">` wrapping the list items
    - For each item in `filteredDocs`:
      - Render `<li>` with a `<a :href="item.url">{{ item.title }}</a>` link
      - The URL is the value from tagIndex keys (e.g., `/Certbot使用指南`, `/` for index.md)
      - Set `:key="item.url"` on each list item for TransitionGroup identity tracking
    - Empty state (D-04): when `isEmpty` is true AND the component has been activated (use a ref flag that becomes true after mount, or check `filteredDocs.length === 0 && selectedTags.size > 0` from the composable), show `<p class="hc-empty">没有匹配标签的文档</p>`
    - When no tags selected, the full list from filteredDocs is shown (composable returns all docs)

    Scoped styling:
    - Style `.FilteredDocList` list items to match the current markdown list appearance (similar VitePress default doc list styling)
    - List items: proper spacing between items, link color inheriting VitePress brand color
    - Empty state: centered text with secondary color, padding for visual breathing room

    TransitionGroup animation classes (D-05):
    - `.list-enter-active, .list-leave-active`: transition all properties 0.3s ease
    - `.list-enter-from, .list-leave-to`: opacity 0, transform translateY(-8px)
    - `.list-move`: transition transform 0.3s ease (for sibling position shift animation)

    Note: Since this is a scoped component, the TransitionGroup name-based CSS classes are scoped automatically by Vue's scoping mechanism. The `list-` prefixed classes within `<style scoped>` will work with TransitionGroup's CSS class resolution when the `name` attribute matches.
  </action>
  <acceptance_criteria>
    - File `docs/.vitepress/theme/components/FilteredDocList.vue` exists
    - Renders a list of document links matching the filteredDocs from the composable
    - When no tags selected, shows all documents (same as current markdown list)
    - When some tags selected, shows only matching documents
    - When no documents match, shows "没有匹配标签的文档" empty state message (D-04)
    - Uses TransitionGroup for fade+slide animation on list changes (D-05)
    - Each item links to the correct document URL from tagIndex
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    FilteredDocList component created with filtered rendering, empty state, and animated transitions. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: 注册 FilteredDocList 并替换 index.md 文档列表</name>
  <files>
    docs/.vitepress/theme/index.ts
    docs/index.md
    docs/.vitepress/theme/custom.css
  </files>
  <read_first>
    @docs/.vitepress/theme/index.ts
    @docs/index.md
    @docs/.vitepress/theme/custom.css
  </read_first>
  <action>
    Three changes in this task:

    **1. theme/index.ts — 全局注册 FilteredDocList 组件:**
    Add an import of `FilteredDocList` from `./components/FilteredDocList.vue`.
    In `enhanceApp`, add `app.component('FilteredDocList', FilteredDocList)` alongside the existing `tag-cloud` registration.
    Follow exact same pattern as the existing `app.component('tag-cloud', TagCloudLayout)` on line 21.

    **2. index.md — 替换 Markdown 文档列表:**
    Read the current file. The section below the hero frontmatter contains:
    ```
    ## 文章列表

    - [Certbot 使用指南](Certbot使用指南.md)
    - [...]
    ```
    Replace the entire list of 14 markdown link items (but NOT the `## 文章列表` heading) with a single line containing `<FilteredDocList/>`.
    The heading `## 文章列表` stays as is — FilteredDocList renders below it.
    Result should be:
    ```
    ## 文章列表

    <FilteredDocList/>
    ```
    Remove the empty line before the heading if present to keep the file clean.

    **3. custom.css — 添加 TransitionGroup 动画全局关键帧:**
    Add a new CSS section at the bottom of custom.css for TransitionGroup list animations (D-05).
    The classes must NOT be scoped since `name="list"` in TransitionGroup resolves to global CSS classes.
    These animation CSS classes are applied by TransitionGroup during enter/leave transitions, relying on the TransitionGroup `name` prop creating `.list-enter-active`, `.list-leave-active`, `.list-enter-from`, `.list-leave-to`, and `.list-move` class names globally.
    The CSS block should:
    ```
    /* FilteredDocList TransitionGroup animations */
    .list-enter-active,
    .list-leave-active {
      transition: all 0.3s ease;
    }
    .list-enter-from,
    .list-leave-to {
      opacity: 0;
      transform: translateY(-8px);
    }
    .list-move {
      transition: transform 0.3s ease;
    }
    ```
    These must have a comment header and follow custom.css existing formatting (2-space indent not needed, just follow existing style).
  </action>
  <acceptance_criteria>
    - FilteredDocList registered globally in `enhanceApp` via `app.component('FilteredDocList', FilteredDocList)`
    - `index.md` contains `<FilteredDocList/>` as content under `## 文章列表`
    - `custom.css` contains TransitionGroup animation classes: `.list-enter-active`, `.list-leave-active`, `.list-enter-from`, `.list-leave-to`, `.list-move` with opacity/transform transitions
    - `npm run build` passes with no errors
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    FilteredDocList is globally registered, index.md uses the component instead of static markdown links, and TransitionGroup animations are defined in global CSS. Site builds successfully.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client-side composable state | All filtering happens in-memory on client; no data leaves browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | FilteredDocList.vue | low | accept | Filtering logic runs client-side on static data from build-time tagIndex. No external input, no user-submitted content, no persistence. Risk is limited to incorrect display. |
| T-04-04 | Information Disclosure | TransitionGroup CSS | low | accept | CSS animations operate entirely on pre-existing DOM elements. No data leakage vector. |
| T-04-SC | Supply Chain | npm installs | low | mitigate | No new npm packages added in this phase. |
</threat_model>

<verification>
1. `npm run build` passes with no errors
2. Manual verification via `npm run dev`:
   - Homepage shows FilteredDocList content (14 document links)
   - Click a tag chip — list filters to matching docs
   - Click another tag — list narrows further (AND logic)
   - Click selected tag — list expands
   - Click "清除" — all tags deselected, full list shown
   - Select tags that exclude all docs — empty state appears
   - Selected tags show box-shadow ring
   - List items animate on filter change
</verification>

<success_criteria>
- Homepage tags are clickable with visual active state
- Document list filters reactively on tag selection
- AND logic correctly narrows results for multiple tags
- Empty state shows when no documents match
- Clear button works
- Stats text updates dynamically
- List animations are smooth
- `npm run build` passes
</success_criteria>

<output>
Create `.planning/phases/04-首页标签交互筛选/04-02-SUMMARY.md` when done
</output>
